# --- Authentication ---------------------------------------------------------
# Provide at least one Gemini API key. Required for /correct, /chat/*, /make_deck.
GEMINI_API_KEY=
# GOOGLE_API_KEY=

# --- Model selection --------------------------------------------------------
# Default model used when requests沒有明確指定。
GEMINI_MODEL=gemini-2.5-flash-preview-09-2025
# 允許的模型清單（逗號分隔）；留空會套用內建白名單。
# ALLOWED_MODELS=gemini-2.5-pro,gemini-2.5-flash

# --- Generation tuning ------------------------------------------------------
# 建議維持預設值，除非需要自訂 LLM 設定。
# LLM_TEMPERATURE=0.1
# LLM_TOP_P=0.1
# LLM_TOP_K=1
# LLM_MAX_OUTPUT_TOKENS=2048

# --- Storage & content ------------------------------------------------------
CONTENT_DIR=data
USAGE_DB_PATH=data/usage.db
# USAGE_DB_URL=postgresql+psycopg://user:pass@host:5432/dbname
QUESTION_DB_PATH=data/questions.sqlite
# QUESTION_DB_URL=postgresql+psycopg://user:pass@host:5432/dbname
# CONTENT_ADMIN_TOKEN=change-me

# --- Prompts ----------------------------------------------------------------
PROMPT_FILE=prompts/prompt.txt
DECK_PROMPT_FILE=prompts/prompt_deck.txt
CHAT_TURN_PROMPT_FILE=prompts/prompt_chat_turn.txt
CHAT_RESEARCH_PROMPT_FILE=prompts/prompt_chat_research.txt
MERGE_PROMPT_FILE=prompts/prompt_merge.txt
FLASHCARD_COMPLETION_PROMPT_FILE=prompts/prompt_flashcard_completion.txt
QUESTION_PROMPT_FILE=prompts/prompt_generate_questions.txt

# --- Logging & diagnostics --------------------------------------------------
LOG_LEVEL=INFO
LLM_LOG_MODE=both
LLM_LOG_PRETTY=true
DECK_DEBUG_LOG=1
GENERATOR_DEFAULT_COUNT=8

# --- Server runtime ---------------------------------------------------------
HOST=0.0.0.0
PORT=8080
